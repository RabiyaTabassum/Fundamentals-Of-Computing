#include<stdio.h>
int main()
{
	int n,i,sum=0;
	printf("enter the numbers:");
	scanf("%d",&n);
	for(i=0;i<=n;i++)
	{
		if(i%1==0)
		sum=sum+i;
	}
	printf("sum of all number series=%d\n",sum);
	return 0;
}
	
